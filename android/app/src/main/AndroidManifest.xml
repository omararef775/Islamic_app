<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 📍 صلاحيات الموقع الجغرافي لحساب أوقات الصلاة -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 🔔 صلاحية إرسال الإشعارات (أندرويد 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 🔄 إعادة جدولة الأذان بعد إعادة تشغيل الهاتف -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 📳 السماح بالاهتزاز مع الإشعار -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ⏰ إيقاظ المعالج لإطلاق الأذان في وقته الدقيق -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        ⏰ صلاحية الجدولة الدقيقة
        ملاحظة: تم حذف USE_EXACT_ALARM عمداً لأن Google Play
        تحظرها على التطبيقات العادية (مقبولة فقط لتطبيقات المنبهات والتقويم)
        SCHEDULE_EXACT_ALARM: يطلبها المستخدم عند الحاجة (متوافق مع Play Store)
        والتطبيق يعمل بخوارزمية Fallback تلقائية إذا لم تُمنح
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:label="رفيق المسلم"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            📬 مستقبل الإشعارات المجدولة من flutter_local_notifications
            يعمل كبريد الإشعارات الذي يستقبل الأذان عند حلول وقته
        -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!--
            🔄 مستقبل إعادة التشغيل: يعيد جدولة الأذان بعد إعادة تشغيل الهاتف
            لأن أندرويد يمسح جميع الإشعارات المجدولة عند إيقاف تشغيل الجهاز
        -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!--
            ▶️ مستقبل أزرار إجراءات الإشعار (Action Buttons)
            مسؤول عن استقبال نقر "إيقاف الأذان" من خارج التطبيق
        -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

    </application>

    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
